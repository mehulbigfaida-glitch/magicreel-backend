export interface GarmentDNA{

  category:string;

  garmentName:string;

  // backward compatibility
  style?:string;

  isTop:boolean;

  isBottom:boolean;

  isOnePiece:boolean;

  isOverlay:boolean;

  isEthnic:boolean;

  isSet:boolean;

}