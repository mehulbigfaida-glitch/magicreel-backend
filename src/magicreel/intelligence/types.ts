export interface GarmentDNA{

  category:string;

  garmentName:string;

  // compatibility fields already used
  style?:string;

  garmentLength?:string;

  confidence?:number;

  layering?:string;

  isTop:boolean;

  isBottom:boolean;

  isOnePiece:boolean;

  isOverlay:boolean;

  isEthnic:boolean;

  isSet:boolean;

}